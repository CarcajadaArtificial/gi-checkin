<%= form_for(ticket_type_conference) do |f| %>
  <% if ticket_type_conference.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ticket_type_conference.errors.count, "error") %> prohibited this ticket_type_conference from being saved:</h2>

      <ul>
      <% ticket_type_conference.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Ticket_Type_id %>
    <%= f.text_field :Ticket_Type_id %>
  </div>

  <div class="field">
    <%= f.label :Conference_id %>
    <%= f.text_field :Conference_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
