<%= form_for(ticket_type) do |f| %>
  <% if ticket_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ticket_type.errors.count, "error") %> prohibited this ticket_type from being saved:</h2>

      <ul>
      <% ticket_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ticketType_price %>
    <%= f.text_field :ticketType_price %>
  </div>

  <div class="field">
    <%= f.label :ticketType_name %>
    <%= f.text_field :ticketType_name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
