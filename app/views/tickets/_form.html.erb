<%= form_for(ticket) do |f| %>
  <% if ticket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ticket.errors.count, "error") %> prohibited this ticket from being saved:</h2>

      <ul>
      <% ticket.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="container_form">
    <%= f.check_box :ticket_preregistered, checked: true, class:"hidden"%>

    <h2 class="form_subtitle">Paso 1: Selecciona un taller por día.</h2>
    <div class="combo_fieldLabel fRow_endEnd">
      <p>19 de octubre</p>
      <%= f.select :ticket_conference1, options_for_select([
        ['Fashion: Andy Benavides', '1'],
        ['Fashion: CEDIM', '2'],
        ['Trends: Levadura', '3'],
        ['Trends: Huntmuse', '4'],
        ['Sports: Athlete Booster', '5'],
        ['Entertainment: Inspiral', '6'],
        ['No voy a asistir', '6']]), {prompt: 'Selecciona una opción'} %>
    </div>

    <div class="combofieldLabel fRow_endEnd">
      <p>20 de octubre</p>
      <%= f.select :ticket_conference2, options_for_select([
        ['Fashion: Polanna / Givehope', '7'],
        ['Fashion: Instituto di Moda Burgo', '8'],
        ['Trends: Ana Tere Canales', '9'],
        ['Trends: Taste n Tell', '10'],
        ['Sports: Abierto GNP Seguros', '11'],
        ['Entertainment: La Sociedad', '12'],
        ['Entertainment: Nebulosa', '13'],
        ['No voy a asistir', '6']]), {prompt: 'Selecciona una opción'} %>
    </div>

    <h2 class="form_subtitle">Paso 2: Llena tu información personal.</h2>
    <p class="caption">Campos requeridos <span class="required">*</span></p>
    <div class="combo_fieldLabel fRow_endEnd">
      <p>Nombre completo <span class="required">*</span></p>
      <%= f.text_field :ticket_name, class:"input", :required => true %>
    </div>

    <div class="combo_fieldLabel fRow_endEnd">
      <p>Correo electrónico <span class="required">*</span></p>
      <%= f.text_field :ticket_email, class:"input", :required => true %>
    </div>

    <div class="combo_fieldLabel fRow_endEnd">
      <p>Matrícula</p>
      <%= f.text_field :ticket_schoolId, class:"input" %>
    </div>

    <div class="combo_fieldLabel fRow_endEnd">
      <p>Sexo</p>
      <%= f.select :ticket_conference2, options_for_select([
        ['Mujer', 'm'],
        ['Hombre', 'h'],
        ['Prefiero no especificar.', 'x']], {prompt: 'Selecciona una opción'} %>
    </div>
    </div>

    <div class="combo_fieldLabel fRow_endEnd">
      <p>Edad</p>
      <%= f.text_field :ticket_age, class:"input" %>
    </div>

    <div class="combo_fieldLabel fRow_endEnd">
      <p>Carrera</p>
      <%= f.text_field :ticket_course, class:"input" %>
    </div>

    <div class="combo_fieldLabel fRow_endEnd">
      <p>Semestre</p>
      <%= f.text_field :ticket_semester, class:"input" %>
    </div>
  </div>

  <div class="f_tCenter">
    <%= f.submit "Enviar", class:"button_main" %>
  </div>
<% end %>
