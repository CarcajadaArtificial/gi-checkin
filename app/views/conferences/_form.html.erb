<%= form_for(conference) do |f| %>
  <% if conference.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(conference.errors.count, "error") %> prohibited this conference from being saved:</h2>

      <ul>
      <% conference.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :conference_name %>
    <%= f.text_field :conference_name %>
  </div>

  <div class="field">
    <%= f.label :conference_date %>
    <%= f.datetime_select :conference_date %>
  </div>

  <div class="field">
    <%= f.label :conference_capacity %>
    <%= f.text_field :conference_capacity %>
  </div>

  <div class="field">
    <%= f.label :conference_attendance %>
    <%= f.text_field :conference_attendance %>
  </div>

  <div class="field">
    <%= f.label :conference_description %>
    <%= f.text_area :conference_description %>
  </div>

  <div class="field">
    <%= f.label :conference_speaker %>
    <%= f.text_field :conference_speaker %>
  </div>

  <div class="field">
    <%= f.label :conference_location %>
    <%= f.text_field :conference_location %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
