<%= form_for(conference) do |f| %>
  <% if conference.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(conference.errors.count, "error") %> prohibited this conference from being saved:</h2>

      <ul>
      <% conference.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cnf_name %>
    <%= f.text_field :cnf_name %>
  </div>

  <div class="field">
    <%= f.label :cnf_ticketNumber %>
    <%= f.number_field :cnf_ticketNumber %>
  </div>

  <div class="field">
    <%= f.label :cnf_capacity %>
    <%= f.number_field :cnf_capacity %>
  </div>

  <div class="field">
    <%= f.label :cnf_occupancy %>
    <%= f.number_field :cnf_occupancy %>
  </div>

  <div class="field">
    <%= f.label :cnf_location %>
    <%= f.text_field :cnf_location %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
