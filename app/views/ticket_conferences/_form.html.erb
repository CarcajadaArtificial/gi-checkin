<%= form_for(ticket_conference) do |f| %>
  <% if ticket_conference.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ticket_conference.errors.count, "error") %> prohibited this ticket_conference from being saved:</h2>

      <ul>
      <% ticket_conference.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ticketConference_assistance %>
    <%= f.text_field :ticketConference_assistance %>
  </div>

  <div class="field">
    <%= f.label :Ticket %>
    <%= f.text_field :Ticket %>
  </div>

  <div class="field">
    <%= f.label :Conference %>
    <%= f.text_field :Conference %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
